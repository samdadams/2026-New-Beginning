tap "homebrew/cask"
tap "homebrew/core"

# Browsers
cask "google-chrome"
cask "opera"
cask "opera-gx"

# Productivity
cask "notion"
cask "canva"

# Media / Audio
cask "spotify"
cask "audacity"
cask "macwhisper"

# Video Editing & Utilities
cask "handbrake"
cask "neat-download-manager"
cask "ccleaner"

# Security
cask "lulu"
cask "lastpass"

# Core Utilities
brew "unzip"    # optional (macOS already includes unzip)

/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
brew bundle --file ~/Desktop/Brewfile


brew install --cask google-chrome
brew install --cask opera
brew install --cask opera-gx
brew install --cask notion
brew install --cask canva
brew install --cask spotify
Ç
Terminal
brew install --cask handbrake
brew install --cask neat-download-manager
brew install --cask ccleaner
brew install --cask lulu
brew install --cask lastpass
brew install unzip

- **Studio One 7**
- **Affinity Photo**
- **Affinity Photo 2**
- **FlowithOS (Beta)**
- **DaVinci Resolve Studio**
- **iZotope RX11**
- **Epidemic Sound** (web only)
- **Indl** (need exact full name; not in Brew)
I can give you direct download links if you want them.

## **⭐ FINAL CHECK**